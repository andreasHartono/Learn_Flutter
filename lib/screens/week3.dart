import 'package:flutter/material.dart';

class Week3 extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Week 3 Widget'),
      ),
      body: Center(
        child: Text('This is Week 3'),
      ),
    );
  }
}
